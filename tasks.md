# 剩余任务（交接清单）

> **本文件是交接清单，不是规范。** 规范以 `docs/` 四份为准（产品设计 / 架构 / 使用手册 / 状态）；
> **实现状态以 `docs/xhunter-status.md` 为准**——里程碑总览在它的 §4.2，每个里程碑的「目标 / 范围 /
> 独立验收的证据 / 依赖 / 不做」在它的 §4.3。本文件只回答「还剩什么、按什么顺序、拿什么验收」。
> **剩余任务清零后即可删除本文件。**

**基线**：`master`。已收口 **MS-1 / MS-2 / MS-3 / MS-4 / MS-6 / MS-7 / MS-12**。
剩余：一期收口 **MS-5**；二期 **MS-8 / MS-9 / MS-10 / MS-11**（依赖关系见 `docs/xhunter-status.md` §4.2）。

---

## 一、每一批都适用的纪律

1. **判据先定死**。阈值、次序、归零条件、边界值必须在**实现之前**写成用例，且期望值一律用**字面量**——
   引用被测常量本身做期望值，会把"常量被改掉"这件事变绿（这种自洽的断言等于没有）。
2. **完成判据（DoD）**：
   - `python scripts/check.py` → **exit 0**（build ＋ vet ＋ test ＋ 目标平台 `linux/amd64` 交叉编译）；
   - `gofmt -l .` 无输出；
   - **文档同批回写**（见第 5 条）。
3. **第二层独立验证用变异法**，且**由另一个人做**（写的人自测不算证据）：
   - 把**阈值 / 次序 / 常量 / 归零条件**当变异对象，逐条报出「变红的用例名」；
   - **某条变异没有任何用例变红，那就是缺口**（或那条断言是恒绿的死断言）；
   - **两条纪律容易踩反**：断言里的字符串要确认在被测请求里**独有**（拿一句"顺手的话"去匹配，很可能与
     提示词/内核条款撞词而恒真）；源码**扫描型**断言要两面都压——既会**假绿**（只扫顶层目录、漏子目录、
     漏导入别名、漏间接调用），也会**假红**（注释或字符串里出现同样的字样就算命中）。
   - 弱断言比没有断言更坏：它会让人误以为不变量被守住了。**咬不住的就如实写进状态文档的缺口区，
     不要塞一条恒绿的用例充数。**
4. **注释自闭环**：代码注释说明"为什么"，**不引用 `FR-x.y` / `INV-n` / 章节号**。
5. **文档回写分工**：里程碑落地**只回写状态文档**（`docs/xhunter-status.md`）；只有**规则变化**
   （字段取值口径、事件字段增减、验收标准改写、新增终止原因）才动设计文档。**事件类型只增不改**。
   > 顺带：状态文档的核心表是「状态索引」，每一项的取值是 `已落地 / 部分具备 / 待接入 / 待补` +
   > 里程碑 + **证据用例名**。落地时把它连同用例名一起改，别只改代码。
6. **提交按逻辑单元分开**，提交信息含「为什么」与具体改动，并附验证一行；**是否推送由投递方决定**。
7. **同一份事实只有一个来源**：能在既有契约/既有通道里表达的，不要再造一条（例如机制侧阈值一律经
   既有的上报通道，不在装配层重抄一遍数字）。**没有消费方就不加字段/接口/可配项。**

---

## 二、剩余任务

### MS-5 门禁落地 ✅ 已完成（2026-09-24）

- **做什么**：把「质量门禁」从规格变成能力——`check` 原语实现 ＋ 清单来源裁决 ＋ 全链护栏。
  详版在 `docs/xhunter-status.md` §4.3 MS-5 与 `docs/xhunter-architecture.md` §7.8。
- **必须先确认的两件事**（不确认就写会返工）：
  1. **`expect` 判据的语义**：现在有「对象 `{kind*, pattern, max, stream}`、在全量输出上算」的方向，
     但 `kind` 的取值域、`pattern` 的匹配口径（正则？子串？大小写？）、`max` 是"最多几条"还是
     "至少几条"、`stream` 是只测 stdout 还是含 stderr——**这些要在开工前逐条定死**，否则同一次交付的
     结论不可复现。
  2. **三条护栏的顺序**（元门禁 / 强度不得降低 / `gate_config_changed` 显式上报）：它们必须在
     **清单生效之前**起作用，且顺序要用「多条同时成立」的用例咬住。
- **关键约束**：清单从**基线 commit** 读（不读工作区——否则模型能改判据）；`argv` 直启、`argv[0]`
  为 shell 一律拒；超时走 `context` ＋ 杀进程组；输出限长保留头尾；非交互；子进程环境最小集
  （**凭据绝不下传**）。**两类失败必须分开**：执行失败（命令不存在/起不来/超时）＝**环境错误（退出 1）**；
  判定不通过＝**质量结论**（不是环境问题）。
- **联动**：门禁通过 → 本轮必提交；未通过 → 抑制后续自动检查点；收尾补跑在**交付提交之前**。

### MS-7 会话恢复 ✅ 已完成（2026-09-24）

已落地，见 `docs/xhunter-status.md` §4.3 MS-7 与 §2 索引（FR-12.1、AC-7、IA-6.2/6.3/6.4、L1-8、H6、usage§6）。
要点：`Prepare` 在 `openRecorder` 之前**纯读**回材料（`Load(root)`）→ 回灌上下文 ＋ 新条件追加 →
从下一轮继续（零工具执行、不重放写操作）；`session_delta` 接线；`material.go` 的 `Load` panic 哨兵已回收为真实现。

### MS-8 扩展接入与符号读写 ✅ 部分完成（2026-09-24）

- **第一件事不是写扩展，而是把符号原语注册回来**：一期已把 `symbol_read` / `symbol_edit` /
  `symbol_rename` 从装配层移出（模型看不到它们），实现与构造器仍保留在 `hunt/symbolic`。
  MS-8 恢复注册只需把三行加回装配层清单（`ext.ExtHost` 形参一直留着），随后才谈扩展实现。
  恢复注册时**同时**要把"工具面恒定"的对照用例补上（现在口径是"同一构建内恒定、环境能力不决定注册、
  随能力接入而扩展"）。
- **必须先确认的两件事**：
  1. **协议形态**：规范指名「MCP ＋ 本地 stdio」，但本项目**零第三方运行时依赖**、三套 provider 协议
     都是**手写线格式**（不加厂商 SDK）。所以要定的是**手写 MCP 的哪个子集**：握手是否实现、
     版本不匹配怎么办、只做 `tools/list` ＋ `tools/call` 吗（`resources`/`prompts`/`sampling` 建议不做
     ——`sampling` 会把模型调用倒灌回核心，与"工具执行不经供应商侧"直接冲突）；
     **以及最核心的一条：扩展侧的工具名与"不新增模型可见工具名"怎么调和**（约定固定私有名，还是按
     能力描述符映射）。
  2. **首发后端形态**：自包含**语法**后端是默认（唯一满足"无外部依赖"、也是所有语言的兜底），LSP 是
     可选、缺失降级并上报精度变化。要定**首发语言清单**与**解析器选型**。注意这条只作用于扩展进程，
     不进核心。
- **会连带影响排期的一条**：语法级后端的 `CanResolve=false`，而 `symbol_rename`（MS-10）需要跨文件
  引用解析——所以「首发只做语法级」意味着 **MS-10 要么顺延到 LSP 后端，要么一期只以"结构化错误"形态
  存在**（不撤回工具名、调用返回结构化错误、**绝不静默降级为文本替换**）。这一点必须在设计确认时拍板。
- **关键约束**：扩展**不新增工具名**；写盘**只在核心**（`ExtHost` 接口不含任何写方法，这是类型级保证）；
  懒启动、随任务回收、崩溃隔离（不得让主循环静默挂起）；仅本地进程、不继承凭据；能力指纹进会话材料、
  不一致**只记录不阻断**。

### MS-9 结构检查（中 ｜ 依赖 MS-5、MS-8）

- **做什么**：把自动检查点的判据从"不可判定"换成真判据（`ParseOK` ＋ 单次编辑的字节区间封闭在某个
  符号内），并补全触发链：**门禁通过 > 模型显式请求 > 结构检查通过 > 都不满足则不提交**。
- **关键约束**：判据**三态保留**（pass / fail / **undecidable** 仍要留给"判据不在"的情形）；
  **绝不能退化成按轮提交**；不可判定时如实上报（已有一条"每次运行最多一条"的降级事件）。

### MS-10 `symbol_rename` 跨文件重命名 ✅ 已完成（2026-09-24）

- 声明 ＋ 全部引用点一次改完；`CanResolve == false` 时**结构化错误、不降级为文本替换**
  （半完成的重命名比失败更坏）。
- 规模上报（`ext.Impact`）进结果摘要/事件/变更说明，**不做阈值**（"改得多就保守拒绝"是偏好而非判据）。
- **同源要求**：上报的规模与落盘的编辑计划必须来自**同一次定位**。

### MS-11 上下文压缩（大 ｜ 依赖 MS-6、MS-7）

- 接上压缩的三档水位与冷却（**不允许"超限才压"**——那时已无余量跑完压缩后的那一轮），分层下压
  L0→L4（默认零模型调用；L4 摘要生成一次即落盘、恢复时读回），接上 `context_compacted` 发出点。
- **保丢优先级**：**永不丢**——任务正文与验收标准、工作区约定、当前轮 messages、**写操作记录**。
- **硬上限那条不要动**（已落地，且"不能静默不报"）。

### 待排期小项（彼此独立，可随时插入）

- **冻结前必须回收的 4 处 panic 哨兵**：`ext/ext.go` 四处（`ExtHost` 的四个方法，随 MS-8）。回收
  方式＝换成「装配期显式失败」或「运行期如实降级并上报」，并补用例。**"走到即炸"只允许存在于未冻结期。**
  > 现状补充：符号原语移出装配层后，这四处在实际运行路径上**已不可达**。`cmd/xhunter/material.go`
  > 那处（会话恢复 `Load`）**已随 MS-7 回收**为真实现（纯读回灌，并补了用例）。
- **写权限取向**（备忘录）：以后由**任务内容**决定可写面（从"目录黑名单"演进为"任务声明的可写面"）；
  引擎自有材料仍强制禁写。
- **静态扫描类断言**（凭据不落事件/日志/补丁/材料、核心不出现供应商名）——属 CI 级，不是单测能覆盖的。
- **嵌套 `AGENTS.md` 的附注**、**信号组合的更多断言**。
- **明确不进一期**：单步驱动。**已决不做**：影响面阈值。
- **多 agent 路线**：目前是**未采纳的备忘录**（`docs/xhunter-memo-multi-agent-route.md`）。若将来要做，
  落点是**操作原语**（循环层不用改）；但其中最硬的一条是"子 agent 的工具面"与已承诺的"同一构建内
  工具面恒定"冲突，**必须先解决该设计冲突再编码**。

---

## 三、几条已经踩过的坑（省得重踩）

- **空数组 ≠ `null`**：一期"没有扩展 / 没有过滤器"是**已知事实**，如实报 `[]`；写成 `null` 会被读成
  "不知道有没有"。
- **"结构保证"与"纪律"要分清**：靠"记得在每条 return 前写终态"的保证迟早会漏；能靠**结构**保证的
  （前置终态 ⇒ 后续分支不可达）就用结构保证，并写一条用例把该结构钉住。
- **同一份事实不要两处算**：事件与结果文件、上报值与实际执行值，都必须读**同一份**；两处各算一遍迟早会漂。
- **跨文档的常量最容易写反**：退出码、编号、取值域这类"到处引用"的东西，改一处要 grep 全仓；
  曾经在同一个项目里把退出码 1/2 写反过三处。
- **文档里引用的用例名要能 grep 到**：验收证据清单里写过从未存在的用例名（3 处）——写清单时逐个核实。
- **重新标定既有用例的参数时要自证不是放宽**：事件条数变了导致测试夹具索引要跟着移时，要单独证明
  "去掉被测实现该用例仍会变红"，否则那就是把断言悄悄放宽了。
